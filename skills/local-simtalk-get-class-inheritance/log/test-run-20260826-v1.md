# Skill Test Run — 2026-08-26

测试目标：实地跑一遍 `local-simtalk-get-class-inheritance` 技能的两段 pipeline，验证端到端可用。

## 1. 环境 / Environment

| 项 | 值 |
|---|---|
| Skill | `skills/local-simtalk-get-class-inheritance/` |
| Server | Plant Simulation 2606.0002（宿主机） |
| TCP port | 50007 |
| Client host | WSL2 容器 → `host.docker.internal:50007` |
| 测试时间 | 2026-08-26 |

## 2. 握手 / Handshake

```bash
$ python3 /root/skills_of_plant_simulation/skills/local-simtalk-execution/scripts/socket_client.py \
    --host host.docker.internal --port 50007 \
    --data '{"type":"ping","action_id":"test001"}||END||' \
    --resp-mode delimiter --resp-delimiter '||END||' --timeout 5
{ "type": "ping", "result": "success" }||END||
```

✅ 链路通。

## 3. 测试用例 / Test Set

12 个候选类（涵盖 6 个 domain + 1 个用户派生类，单 batch 跑完）：

| # | Path | 域 | 期望 Origin |
|---|---|---|---|
| 1 | `.MaterialFlow.Station` | MaterialFlow | `VOID` |
| 2 | `.MaterialFlow.Conveyor` | MaterialFlow | `VOID` |
| 3 | `.MaterialFlow.Connector` | MaterialFlow | `VOID` |
| 4 | `.MaterialFlow.Source` | MaterialFlow | `VOID` |
| 5 | `.MaterialFlow.Buffer` | MaterialFlow | `VOID` |
| 6 | `.Fluids.Tank` | Fluids | `VOID` |
| 7 | `.Fluids.Pipe` | Fluids | `VOID` |
| 8 | `.Resources.Worker` | Resources | `VOID` |
| 9 | `.Resources.Workplace` | Resources | `VOID` |
| 10 | `.InformationFlow.FileInterface` | InformationFlow | `VOID` |
| 11 | `.MUs.Part` | MUs | `VOID` |
| 12 | `.UserObjects.MyFrame.Station` | UserObjects (derived) | `.MaterialFlow.Station` |

写入 `test_paths.txt`。

## 4. Step 1 — `probe_inheritance.py`

```bash
$ python3 scripts/probe_inheritance.py test_paths.txt data/test_run.tsv
```

**结果**：

- ✅ 单 batch（12 paths / batch ≤ 12 limit）跑完
- ✅ 12 行 TSV 写入 `data/test_run.tsv`，所有路径命中
- ✅ JSON 形式也打印到 stdout（含 last batch 的 12 entries）

**TSV 内容**：

```
.MaterialFlow.Station<TAB>Station<TAB>Station<TAB>VOID<TAB>.MaterialFlow.Station<TAB>VOID
.MaterialFlow.Conveyor<TAB>Conveyor<TAB>Conveyor<TAB>VOID<TAB>.MaterialFlow.Conveyor<TAB>VOID
.MaterialFlow.Connector<TAB>Connector<TAB>Connector<TAB>VOID<TAB>.MaterialFlow.Connector<TAB>VOID
.MaterialFlow.Source<TAB>Source<TAB>Source<TAB>VOID<TAB>.MaterialFlow.Source<TAB>VOID
.MaterialFlow.Buffer<TAB>Buffer<TAB>Buffer<TAB>VOID<TAB>.MaterialFlow.Buffer<TAB>VOID
.Fluids.Tank<TAB>Tank<TAB>Tank<TAB>VOID<TAB>.Fluids.Tank<TAB>VOID
.Fluids.Pipe<TAB>Pipe<TAB>Pipe<TAB>VOID<TAB>.Fluids.Pipe<TAB>VOID
.Resources.Worker<TAB>Worker<TAB>Worker<TAB>VOID<TAB>.Resources.Worker<TAB>VOID
.Resources.Workplace<TAB>Workplace<TAB>Workplace<TAB>VOID<TAB>.Resources.Workplace<TAB>VOID
.InformationFlow.FileInterface<TAB>FileInterface<TAB>FileInterface<TAB>VOID<TAB>.InformationFlow.FileInterface<TAB>VOID
.MUs.Part<TAB>Part<TAB>Part<TAB>VOID<TAB>.MUs.Part<TAB>VOID
.UserObjects.MyFrame.Station<TAB>Station<TAB>Station<TAB>.MaterialFlow.Station<TAB>.MaterialFlow.Station<TAB>.MaterialFlow.Station
```

每行 6 个字段：`path`, `name`, `type`, `origin`, `originroot`, `cls`。

## 5. Step 2 — `render_inheritance_map.py`

```bash
$ python3 scripts/render_inheritance_map.py data/test_run.tsv data/test_run.json
```

**Stdout 输出**：

```
Total classes captured: 12
  Root classes (Origin=VOID): 11
  Derived classes (Origin!==VOID): 1

================================================================================
INHERITANCE MAP (parent -> children)
================================================================================

.MaterialFlow.Station  (1 child class)
  ├─ .UserObjects.MyFrame.Station  [Station]

VOID  (11 child classes)
  ├─ .MaterialFlow.Station  [Station]
  ├─ .MaterialFlow.Conveyor  [Conveyor]
  ├─ .MaterialFlow.Connector  [Connector]
  ├─ .MaterialFlow.Source  [Source]
  ├─ .MaterialFlow.Buffer  [Buffer]
  ├─ .Fluids.Tank  [Tank]
  ├─ .Fluids.Pipe  [Pipe]
  ├─ .Resources.Worker  [Worker]
  ├─ .Resources.Workplace  [Workplace]
  ├─ .InformationFlow.FileInterface  [FileInterface]
  ├─ .MUs.Part  [Part]

================================================================================
DERIVED CLASSES (the user-defined classes that inherit from a built-in)
================================================================================

  .UserObjects.MyFrame.Station  [Station]
    Origin        = .MaterialFlow.Station
    OriginRoot    = .MaterialFlow.Station
    Class         = .MaterialFlow.Station

Wrote data/test_run.json
```

**JSON 内容**（关键字段）：

| 字段 | 值 |
|---|---|
| `total_classes` | 12 |
| `root_classes` 长度 | 11 |
| `derived_classes` 长度 | 1 |
| `tree` keys | `[".MaterialFlow.Station", "VOID"]` |
| `derived_classes[0]` | `{"path": ".UserObjects.MyFrame.Station", "name": "Station", "type": "Station", "origin": ".MaterialFlow.Station", "originroot": ".MaterialFlow.Station", "class": ".MaterialFlow.Station"}` |

## 6. 验证 / Verification

| ID | 检查 | 期望 | 实际 | 结论 |
|---|---|---|---|---|
| TR-01 | 链路 ping | `result:"success"` | ✅ | ✅ |
| TR-02 | probe 12 paths | 12 行 TSV | 12 行 | ✅ |
| TR-03 | 所有 path 命中 | 0 缺失 | 0 缺失 | ✅ |
| TR-04 | 11 个 root Origin=VOID | True | True | ✅ |
| TR-05 | 1 个 derived Origin≠VOID | True | True（`.UserObjects.MyFrame.Station`） | ✅ |
| TR-06 | derived class 的 OriginRoot | `.MaterialFlow.Station` | `.MaterialFlow.Station` | ✅ |
| TR-07 | derived class 的 class | `.MaterialFlow.Station` | `.MaterialFlow.Station` | ✅ |
| TR-08 | JSON top-level keys | 5 个 | `['captured_at','derived_classes','root_classes','total_classes','tree']` | ✅ |
| TR-09 | derived_classes[0] 字段 | 6 个 | `path/name/type/origin/originroot/class` 全有 | ✅ |
| TR-10 | tree 含 VOID sentinel | True | True（11 个 root 在 VOID 键下） | ✅ |
| TR-11 | py_compile probe | 0 | 0 | ✅ |
| TR-12 | py_compile renderer | 0 | 0 | ✅ |

## 7. 结论 / Conclusions

1. **Pipeline 端到端可用 ✅** — `probe_inheritance.py → TSV → render_inheritance_map.py → JSON` 跑通，无报错。
2. **v15 readlog workaround 仍稳定 ✅** — 单 batch 12 paths 一次过，未触发 buffer 截断。
3. **Schema 与 SKILL.md 一致 ✅** — `derived_classes[0]` 的 6 字段全部为 `class`（不是 `cls`），与 tree 子节点保持一致。
4. **VOID sentinel 行为正确 ✅** — `tree["VOID"]` 容纳 11 个 root classes，便于下游消费者用单一索引定位所有"无父类"的节点。

## 8. 输出物 / Artifacts

| 路径 | 说明 |
|---|---|
| `test_paths.txt` | 12 个测试路径 |
| `data/test_run.tsv` | probe 输出的 TSV |
| `data/test_run.json` | renderer 输出的 JSON |
| `log/test-run-20260826-v1.md` | 本文件 |

## 9. 复现命令 / Reproduction

```bash
cd /root/skills_of_plant_simulation/skills/local-simtalk-get-class-inheritance

# 1. Probe (1 batch, 12 paths)
python3 scripts/probe_inheritance.py test_paths.txt data/test_run.tsv

# 2. Render
python3 scripts/render_inheritance_map.py data/test_run.tsv data/test_run.json

# 3. Sanity-check
python3 -c "
import json
d = json.load(open('data/test_run.json'))
print('total =', d['total_classes'],
      'roots =', len(d['root_classes']),
      'derived =', len(d['derived_classes']))
"
```