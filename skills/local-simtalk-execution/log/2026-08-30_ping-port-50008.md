# Usage log — 协议级 ping 确认 host.docker.internal:50008 联通

**Date:** 2026-08-30  **Skill:** `local-simtalk-execution`
**Target:** host.docker.internal:50008  **Mode / Action:** ping  **Operator:** plant-simulation-expert

## Goal
用户要求测试 Plant Simulation 连通性。最初按默认端口 50007 探测 `REFUSED`；用户纠正端口为 **50008**，重做 pre-flight + 协议级 ping 两层验证。

## Steps
1. TCP 层探测（pre-flight，铁律❶）：
   `python3 -c "socket.connect(('host.docker.internal', 50008))"` → `CONNECTED`
2. 协议层探测（避免 SimTalkClaude 对空 payload 不回包，必须真实 JSON）：
   ```bash
   python3 skills/local-simtalk-execution/scripts/socket_client.py \
     --host host.docker.internal --port 50008 \
     --data '{"type":"ping","action_id":"connectivity-test-001"}||END||' \
     --resp-mode delimiter --resp-delimiter '||END||' --timeout 10
   ```
3. 回复：`{ "type": "ping", "result": "success" }||END||`

## Result
- TCP: PASS（50007 是错的，50008 才是运行端口）
- 协议: PASS（`result:success`，`||END||` 分帧 OK）
- 退出码 0

## Verdict — PASS
两层验证通过，SimTalkClaude 服务在 host 50008 端口正常监听 + 响应 JSON。

## What this run validated / learned
- **本会话端口约定 = 50008**，不是 SKILL.md 默认示例的 50007。后续所有走 TCP 的技能调用（write / read / add-note / modify-attribute / class-management / read-library / get-folder-tree / get-class-inheritance / os-functions / create-method-object）都需要 `--port 50008`，或 `export SIMTALK_PORT=50008` 后让脚本读取环境变量。
- 协议分帧用 `||END||`（与 SKILL.md §2 / lifelines.md §2 一致），无需调整。
- ping 用最小 JSON `{"type":"ping","action_id":"<id>"}` 就够，不需传其它字段。
- 行为符合预期：第一次 50007 探测 REFUSED 时确为端口错（不是网络问题），第二次 50008 立即 PASS。
