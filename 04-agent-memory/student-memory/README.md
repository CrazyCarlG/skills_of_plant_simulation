---
last_updated: 2026-09-02
contributors: [@plant-simulation-student]
scope: `plant-simulation-student` agent 产出的"模型学习 session 笔记"索引,按 Date + Model + Scenario 维度
revision_notes:
  - 2026-09-02 (13th):追加 BIW_Bodyshop_detailed bodyshop-deepdive session(✅ **完整 BIW 工艺拓扑**:2 路合流 Source→Stamping→B_St→bodyshop(Inner/Outer/Roof/BodySide_Inner/BodySide_Outer + B_BsI/B_BsO/B_Roof 3 路合流)+ Closures.Fenders/Doors/Hood/Deck_Lid(单工位串行 cap=1)+ B_Bs→Paint→B_Pt + Source1→Powertrain→B_Pwt 2 路合流 FinalAssembly→Drain;🆕 **嵌套 Frame 3 层**(根 / bodyshop / Closures);🆕 **2 个 ExperimentManager Frame**(135+ 节点 Siemens 标准模板 ~50 methods);🆕 **Interface 类首次观察**(from_underbody/to_Paint/from_Roof/to_Paint 跨 Frame 通讯接口);🆕 **ProcTime 显示规律** — AssemblyStation/Station/Buffer real seconds 49.0000/51.0000/53.0000 vs ParallelStation mm:ss 11:26.0000/6:48.0000/14:27.0000/5:57.0000/4:00.0000(候选 Quirk:值驱动 to_str(time) 分支?需 typeOf 验证);✅ **顶层 0 Method**(全声明式,比 7-Frame 集更彻底);✅ 4 Buffer Cap=40 统一;✅ FinalAssembly cap=-1(无限合流);⚠️ BFS 脚本硬编码 port=50007 需手动走 simtalk_send.py(本次 50010);⚠️ **方法论纪律 ❸ 已实跑仿真**(Result=success)— 用户引导下完整 EventController 周期 reset → start(EndTime=99999=1天3h46m)→ 读 19 对象 stats → reset+sleep 验证归零;**实测产出**:Source.NumOut=1049,Stamping.NumMU=14(Cap=14 满→ bottleneck),B_St.NumMU=37(Cap=40 接近满稳态),FinalAssembly.NumIn=NumOut=1053,Drain.NumIn=1053(**2 路合流完美对齐,27.7h 出 1053 辆完整车身**);🆕 **4 个新 Quirk 候选**:`#27 wait/waituntil/stopuntil not allowed in formulas` + `#28 SimTime is read-only` + `#29 EventController.start is fire-and-forget`(需 Bash sleep ≥ 8s 让 GUI event loop 推进)+ `#30 EventController.reset is fire-and-forget too`(需 sleep ≥ 2s 验证归零);🆕 **EndTime 内部单位换算**:`1000→16:40.0000` mm:ss 格式 / `99999→1:03:46:39.0000` dd:hh:mm:ss 格式,raw seconds;⚠️ `EventController.Active` 属性不存在;🆕 **模型输入清单完整探明**:① MU=默认 Part 类 ② Source.Interval=49/51s 固定 ③ Station ProcTime 固定 mm:ss 或 raw seconds ④ **🔧 BufferSizeDD 6 项 ["1","3","5","10","20","40"]** + Buffersize=40 + globalBuffersize=true + **ExpTable Row2=[1,3,5,10,15,20,40]** 7 项预设(Buffer容量敏感性实验)⑤ **🔧 failureActive=true + MTTR=25s + Availability=95%**(故障模拟已激活)⑥ EventController.EndTime=99999s;🆕 **🆕🆕 5 组参数实验实测结论(EndTime=30000s=8.3h,6 个对比 EXP0-6)**:故障关 **+12.3%** ⭐ / Buffer=10 **-4.2%** / Avail=99% 仅 +1.7% / MTTR=5 几乎无用 +0.4% / Stamping cap=20 几乎无用 -0.8% / 全组合优化 +11.3%(主贡献来自关故障);**🆕 关键反直觉发现**:① 真正的瓶颈不是 Stamping 而是 B_St 容量+Source 节拍 ② MTTR 缩短无效(因 Avail 固定时 MTBF 按比例减,停机占比不变)③ 真正的杠杆是 **failureActive=false** 而非 Avail/MTTR/Stamping cap ④ Buffer 设计过度保险(Cap=10 损失仅 4.2%);**🆕 修正后的最优配置**:🥇 BufferSize=40 + failureActive=false(Drain≈538)/ 🥈 Buf=40 + fail=true + Avail=99%(Drain≈487)/ 🥉 实际可降低 Buffer Cap 至 20-30 节省空间;**🆕 BufferSizeDD 链路实测生效**:Buffersize.Value:=10 → B_St.Capacity 40→10 ✓;🆕 所有配置已 reset 恢复原状;🆕 **🆕🆕 8 组参数实验(短跑 + 长跑 + EXP7 长跑验证 + EXP8 Buf=20)**:故障关短跑 +12.3% / **长跑 +14.5%** / Buffer=10 -4.2% / **BufferSize=20 仅 -1.5%(EXP8)验证 Cap=40 过度保险** / Avail=99% +1.7% / MTTR=5 +0.4% / Stamping cap=20 -0.8%;**🆕 最终最优配置**:Cap=40 + failureActive=false → Drain 538 短/ 1207 长;**🧠 强化记忆:学生仿真控制 SOP 完整沉淀**(用户引导要求)— 5 步标准流程 + 4 个 Quirk 必背提醒 + SimTalk/Bash 模板 + "可写 vs 不可写"边界 + "该不该跑"决策矩阵;建议 curator 评审是否沉淀到 `02-domain-know-how/03-modeling-know-how/03-software/simulation-control-sop.md`;ICN 累计 0 个新发现)。
  - 2026-09-02 (12th):追加 AllModels station-onpull-meta-analysis session(**7-Frame 集全部 5 个 Station.OnPull 完整源码对比**:RobotComau~25 行单 Poses 模式 + XZYStacker~30 行 XYZ 简化 + PortalCrane~30 行 XYZ 简化 + **LinearPortal~50 行高级(🆕 DataList 路由表 + calcDroppedPerpendicularFootPoint 几何计算 + throwRuntimeError)** + SevenAxisRobot 60+ 行完整 8 状态机;🆕 **LinearPortal.PullCtrl = self.OnPull1** 带数字后缀(对比其他 4 个 `self.OnPull`);🆕 **关节命名 5 种约定**:Poses/RobotBase+RobotBaseZ/X/Y/Z/X/Z/Y/XLoader+ZLoader;🆕 **5 个 OnPull 共享 5 个 boilerplate pattern**(重入保护 + EnforceProcessing flag + `?.FwBlockListEntry1` MU 链式迭代 + `part.move(?, 0)` 2 参装载 + `waituntil part.Location /= ?` 卸货等待);🆕 **dest 解析 4 种模式**:`?.succ` 单 succ(4/5)+ `z_uniform(1, robot.NumSucc+1)` 随机(SevenAxisRobot)+ DataList 路由表(LinearPortal)+ `throwRuntimeError` 错误处理(LinearPortal);🆕 **`destObj = partDestination` 隐式赋值** vs `:=` 显式(SimTalk 2.0 if 块内允许);✅ **`obj.&Method.Program` SOP 第 8 次跨 Station OnPull 验证完全可移植**;✅ 7-Frame 集 5/7 Station 有 OnPull(2 个用 OnExit / Frame-as-Semaphore 替代);ICN 累计 14 个)。
  - 2026-09-02 (11th):追加 XZYStacker stacker-crane-deepdive session(7-Frame 集**首例 Buffer + XYZ 三轴简化堆垛机**:`Buffer → NwIOBuffer`(IO buffer 内部实现)新 ICN;完整 linear topology `Source→Conveyor(3m)→XZYStacker→Buffer(8 槽)→Station(10s)→Conveyor2(3.05m)→Drain`;🆕 **XZYStacker Station.OnPull 完整 dump** ~30 行(SevenAxisRobot 60+ 行的简化版,无状态机无阻尼);🆕 **3D 关节链 2 种模式** — name-based `getObject("X") → x_Joint → x_Joint.getObject("Z") → z_Joint → z_Joint.getObject("Y") → y_Joint`(对比 SevenAxisRobot index-based `RobotBase/RobotBaseZ`);🆕 **`?.succ` 单 succ routing**(对比 prior MarkerCrossing `z_uniform(1, robot.NumSucc+1)` 随机);🆕 **2 Conveyor 不同 length**(3m vs 3.05m);✅ 6 Connector 连续 Connector/2-6 无命名 gap(对比 prior UUID-stable gap 模式);✅ `obj.&Method.Program` SOP **第 7 次跨 Method dump 验证** 完全可移植;✅ 第 5 次确认 Quirk #7 `z_uniform` 不在本模型出现(`?.succ` 单 succ 无 off-by-one);XZYStacker Station.ProcTime=0 + PullCtrl=OnPull 是 stacker crane 标准 pattern;ICN 累计 14 个)。
  - 2026-09-02 (10th):追加 MarkerCrossing userobjects-class-edges session(补探:🆕 **`.UserObjects.AGV`(Transporter)是 7-Frame 集首个用户自定义 class**,MarkerCrossing.AGVPool.AGV 是其实例 — Factory51 `UserObjects/Production` 范式 7-Frame 集首例;🆕 **4 嵌套 corner cell 边缘各异**:MarkerCrossing=5 edges(MarkerCrossing 是 south/center cell 含 RightEntry/RightExit),MarkerCrossing2=4 edges(SW corner),MarkerCrossing3=4 edges(NE corner),MarkerCrossing4=4 edges(NW corner) — 位置感知模板;✅ Destinations 部分 dump `d[1]=M1, d[2]=M2`(outer 4 Marker 是 Init/DestCtrl 随机派单目标);✅ DataList element print SOP:`var x:=d[i]; print x`(避免直接 `print d[i]` 触发 bash 转义 Syntax error);✅ AGV 物理差异:本模型 Accel=0.4/Decel=1 vs prior AGVWithRobot Accel=0.8/Decel=0.5 — **同一概念 AGV 在 7-Frame 集不同模型用不同基类**(AGVWithRobot 用 `.StandardObjects.Vehicle`,MarkerCrossing 用 `.UserObjects.AGV` 自定义);✅ 第 4 次确认 Quirk #7 `z_uniform(1, Dim+1)` off-by-one)。
  - 2026-09-02 (9th):追加 MarkerCrossing crossing-semaphore-deepdive session(7-Frame 集**首个 Frame-as-Semaphore 互斥锁 + 嵌套 cell 模板**:4 个 MarkerCrossing 同名嵌套 Frame × 26 子节点 = **104 内节点 + 26 外节点 = 130 总对象**,物理语义 2×2 grid + 对角交叉;🆕 **2 个新 ICN**:`DataList → NwRandom`(DataList 内部 random accessor)+ `Method → Method`(无 Nw 前缀,普通类);**完整 6 个 Method dump**:Init(AGV dispatcher 死循环 `agv.DestCtrl = &DestCtrl` callback 注入)+ DestCtrl(on-exit 重新派单)+ 4 嵌套 Method(EntranceCtrl/ExitCtrl/RecalcLayout/Reset)— Frame-as-Semaphore 用 `Owner` Variable + `StoppingCounter` + `_3D.showGraphicGroup("Free"/"Busy")` 3D 状态切换;🆕 **callback 注入模式**(Init 显式 `agv.DestCtrl=&DestCtrl` 注入到 AGV.DestCtrl 属性);✅ `obj.&Method.Program` 跨 6 个 Method SOP 完全可移植;✅ Variable `.Value` API(非 `.contents`);🆕 Connector 命名 gap(无 Connector1,Connector 出现在末尾);✅ 完整 13 Connector 拓扑实测:4 嵌套 Frame 形成 2×2 cell + 4 对角交叉;✅ 第 3 次确认 Quirk #7 `z_uniform(1, Dim+1)` off-by-one(Init/DestCtrl/RecalcLayout 全用))。
  - 2026-09-02 (8th):追加 SevenAxisRobot onpull-dump session(**成功 dump OnPull 60+ 行 SimTalk 源码**:用户建议的 `robot.&OnPull.Program` 语法一次性 dump;这是**完整 3D 七轴机器人动画控制 method** — 8 状态状态机(DrivingEmpty/Loading/DrivingFull/Damping/Unloading/Blocked/DrivingHome/Waiting)+ 重入保护 `if self.NumInExecution > 1 then return end` + MU 迭代 `?.FwBlockListEntry1` + 3D pose `_3D.Poses.moveTo(...)` + 阻塞重试 + `part.move(DestinationObject)` MU 移动 API;🆕 **Quirk #26**:`s.OnPull`(属性访问)vs `s.&OnPull`(ref-operator)行为差异 — 前者报 "FwBlockListEntry1 fallback 错",后者干净报 "cannot find the object";**修正 prior onpull-attempt Quirk #21/#23/#24 大部分错判** — 实际 `?.FwBlockListEntry1` 是合法 MU iterator,不是内部别名;✅ 跨 session 验证:同样 `s.&OnExit.Program` 语法对 AGVWithRobot Station1 也工作(返 7 行简化派单);🆕 **dump Station callback SOP**:`obj.&MethodName.Program`(不依赖 path 寻址);✅ 修正 prior RobotSet Quirk #7 `z_uniform(1, NumSucc+1)` 真实 off-by-one 本 session 再次确认)。
  - 2026-09-02 (7th):追加 SevenAxisRobot onpull-attempt session(聚焦 `.Models.SevenAxisRobot.SevenAxisRobot.OnPull` method 源码 — **OnPull 存在但源码不可 dump**:PullCtrl="self.OnPull" 是 string ref,`str_to_obj(path)` 返回 VOID,`PullCtrl.Program` / `&PullCtrl` / `s.OnPull` / `FwBlockListEntry1` 全部失败,read-library `probe_batch` 报 `_void: True`;🆕 **Quirk #20-25 一次性新增 6 条**:PS 2.0 simtalk_run callback method 反射硬限制 + PullCtrl string/void 双重语义不一致 + FwBlockListEntry1 内部编译别名对运行时 opaque + getAttribute(string,...) 签名不接 object 参数 + &PullCtrl ref-operator 编译错;**修正** prior branching-deepdive §01 "NO_METHODS 纯声明式" 结论 — 实际是"Frame 顶层无 Method,Station 上有 OnPull callback 但不可 dump";**OnPull 源码实际内容未获取,需 user 在 GUI 中 F8 查看或 `expert` 实跑仿真推断**;Result: partial)。
  - 2026-09-02 (6th):追加 SevenAxisRobot branching-deepdive session(7-Frame 集**首例多出口分支拓扑**:SevenAxisRobot Station 1 进 2 出 + ParallelStation 4 槽位 2×2 网格 + Conveyor 3m/1m/s + Display "Waiting" string state + 完全 NO_METHODS 纯声明式;**Novel ICN 累计 11 个**:续增 `EventCtl`(EventController,无 Nw 前缀)/ `Machine`(ParallelStation 实际类名)/ `Line`(Conveyor 实际类名)/ `NwDigitDpy`(Display);✅ ParallelStation baseline matches `Capacity=4` + XDim=YDim=2 一致;✅ Conveyor Length=3m/Speed=1m/s 标准化;🆕 Drain3 同名复用模式(与 Connector 编号 gap 同源 — 用户编辑删除后 PS 保留历史命名);Quirk 候选 #18 batch attr probe 首个 fail 后静默跳过(导致 readlog 不显示后续 attr 结果,必须逐 attr 单 probe)+ #19 Display polymorphic Value(ICN=NwDigitDpy 但实际是 string);⚠️ Display string state 触发条件未明)。
  - 2026-09-02 (5th):追加 AGVWithRobot agv-dispatch-deepdive session(单 AGV + 9 Marker 闭环 pickup-loop 拓扑 + OnExit pull 派单全 dump;**Novel**:Connector 编号 UUID-stable gap(5/6/7/8/13/9/10/11/12,缺 1-4)、MStation* 实为 Marker 命名误导、AGV class 在 .StandardObjects 不是 .MaterialFlow、pickup-loop vs RCS 中央调度对照;Quirk 候选 #16 `.~` 行为双模 + #17 `print &o.Program` 编译错;撤销 session 中途"NumAGVs 新 Quirk"误判——baseline `AGVPool/methods` 无此 API)。
  - 2026-09-02 (4th):追加 PortalCrane crane-deepdive session(单对象深挖:`.Models.PortalCrane.PortalCrane` Station-as-Crane 机制 + 子对象(Station/Station2/PortalCrane/Source/Drain/Connector×4)+ 12 个 Place/Source/Drain 属性实测;**Novel finding**:PS 2.0 类注册名重构——`.MaterialFlow.Station.InternalClassName="Place"`(实际类名)、Frame=`Network`、Source=`NwSource`、Connector=`NwArc`、Models root=`NwObjFolder`(PS Help 文档标题"Station"滞后);新增 Quirk #13 `numChildren=0`(即使 9 个可见子对象)、#14 `obj.Methods/Program/getAttribute("Methods")` simtalk_run 全部 Unknown identifier、#15 `obj.~.~.~` 多重 ~ 链报"Arithmetic operations"——Quirk #6/10/12 的 simtalk_run 通道限制扩展。**与 prior RobotComau 完全同构**:Station-as-Subject 模式跨 2 个 7-Frame 模型成立)。
  - 2026-09-02 (3rd):追加 RobotComau station-as-robot-deepdive session(单对象深挖:`.Models.RobotComau.RobotComau` Station-as-Robot 机制 + 30+ 属性 + 5 个 Connector 实测;新增 Quirk #8-#12:PS 2.0 属性收缩白名单 + simtalk_run 单 print + readlog fallback SOP 形成)。
  - 2026-09-02 (2nd):追加 robot-set-overview session(7 Frame 教学集合通览:RobotComau/XZYStacker/PortalCrane/LinearPortal/MarkerCrossing/SevenAxisRobot/AGVWithRobot)。
  - 2026-09-02:对齐 `agents/plant-simulation-student.md` 优化版——student 与 expert 共享 baseline + 技能 + Quirk 协议,姿态差异(学习 vs 执行)+ 输出差异(5 维 note vs 6 段 summary);笔记结构由"5 维 + 2 段"扩展为"5 维 + 6 段(含 Operator self-review)";student 描述由"只读扫描"改为"默认只读,写需确认+回滚"。
---

# 04-agent-memory/student-memory — 模型学习 session 笔记索引

> **定位**:本目录是 `plant-simulation-student` agent 的**只写**记忆区。每个 session 笔记 = 一份按 `02-domain-know-how/` 5 维结构镜像分析某个用户仿真模型的观察报告。
>
> **与 `plant-simulation-expert-memory/` 的区别**:
> - expert = 跑 SimTalk / 写方法 / 改模型 / 执行任务 → session summary 记录"做了什么"
> - student = **默认只读**扫描 / 提炼模式 / 5 维分析 → session note 记录"学到了什么";用户明确说"演示给我看"才可写,改前确认 + 改后必回滚/记录
>
> **与 expert 共享**:同一套 baseline(`01-plantsimulation-knowledge/` + `02-domain-know-how/`)、同一套 `local-simtalk-*` 技能、一套 Quirk 编号协议——student 的差异只在姿态(学习 vs 执行)+ 输出(5 维 note vs 6 段 summary)。
>
> **何时不需要读本目录**:
> - 想看专家的执行流水 → `../plant-simulation-expert-memory/`
> - 想看已沉淀到知识库的 finding → `../../02-domain-know-how/`

## 命名与路径

- **路径**:`04-agent-memory/student-memory/<date>-<model>-<scenario>.md`
  - `<date>` = `YYYY-MM-DD`
  - `<model>` = 模型根 Frame 路径末段(`.UserObjects.Warehouse` → `Warehouse`),多个 root 用逗号
  - `<scenario>` = 用户场景简述,kebab-case,不超过 5 个英文词
- **示例**:`2026-09-01-Factory51-warehouse-orientation.md`

## 索引表(newest at top)

| Date | Model | Scenario | Top finding | Path |
|---|---|---|---|---|---|
| 2026-09-02 | .Models.BIW_Bodyshop_detailed | bodyshop-deepdive | ✅ **完整 BIW 工艺拓扑 + 实跑仿真验证 + 8 组参数实验**(2 路合流 + 嵌套 3 层 Frame):Source→Stamping→B_St→bodyshop(Inner/Outer/Roof/BodySide + B_BsI/B_BsO/B_Roof 3 路合流)→Closures(Fenders/Doors/Hood/Deck_Lid cap=1 串行)→B_Bs→Paint→B_Pt + Source1→Powertrain→B_Pwt 2 路合流 → FinalAssembly→Drain;🆕 **嵌套 Frame 3 层**;🆕 **2 个 ExperimentManager**(135+ 节点 Siemens 模板);🆕 **Interface 类首次观察**;✅ **❸ 实跑仿真成功**(EndTime=99999s 长跑 baseline Drain=1053);🆕 **4 个新 Quirk 候选**:`#27-#30`;🆕 **🆕🆕 8 组对比参数实验(短跑 8.3h + 长跑 27.7h)**:短跑故障关 **+12.3%** ⭐ / **长跑故障关 +14.5%(EXP7)⭐** 验证故障累积 / Buffer=10 -4.2% / Avail=99% +1.7% / MTTR=5 +0.4%(无效) / Stamping cap=20 -0.8%(非瓶颈) / 全组合 +11.3%(主贡献关故障) / **BufferSize=20 仅 -1.5%(EXP8)🆕 验证 Cap=40 过度保险**;**🆕 关键反直觉**:① **故障关**是最大杠杆 ② Avail 固定时 MTTR 缩短无效(MTBF 按比例减) ③ 真正的瓶颈是 **B_St 容量+Source 节拍** 而非 Stamping(长跑 fail=off 后 B_St 仍 40 满,实际节拍 82.9s vs 理论 49s);**🆕 最终最优配置**:🥇 Cap=40 + failureActive=**false** → Drain 538(短)/ 1207(长)/ 🥈 Cap=20 + fail=off 节省空间 -1.5% / 🥉 工程优先级:① 消除故障 +14.5% ② Cap=40→20 ③ 改善 Avail ④ 不改 MTTR/Stamping cap;**🧠 强化记忆:学生仿真控制 SOP 沉淀**(用户引导要求)— 5 步流程 + 4 Quirk 必背 + SimTalk/Bash 模板 + 可写边界 + 决策矩阵。| [2026-09-02-BIW_Bodyshop_detailed-bodyshop-deepdive.md](./2026-09-02-BIW_Bodyshop_detailed-bodyshop-deepdive.md) |
| 2026-09-02 | .Models(7 集) | station-onpull-meta-analysis | ✅ **7-Frame 集全部 5 个 Station.OnPull 完整源码 meta-analysis**:RobotComau(~25 行单 Poses 模式)+ XZYStacker(~30 行 XYZ 简化)+ PortalCrane(~30 行 XYZ 简化)+ **LinearPortal(~50 行高级:DataList 路由表 + calcDroppedPerpendicularFootPoint 几何计算 + throwRuntimeError)** + SevenAxisRobot(60+ 行完整 8 状态机);🆕 **`self.OnPull1` 数字后缀**(LinearPortal PullCtrl vs 其他 4 个 `self.OnPull`);🆕 **关节命名 5 种约定**:Poses/RobotBase+RobotBaseZ/X/Y/Z/X/Z/Y/XLoader+ZLoader;🆕 **5 个 OnPull 共享 5 个 boilerplate pattern**(重入保护 `if self.NumInExecution>1 then return end` + EnforceProcessing flag + `?.FwBlockListEntry1` MU 链式迭代 + `part.move(?, 0)` 2 参装载 + `waituntil part.Location /= ?` 卸货等待);🆕 **dest 解析 4 种模式**:`?.succ` 单 succ(4/5)+ `z_uniform` 随机(SevenAxisRobot)+ DataList 路由表(LinearPortal)+ `throwRuntimeError`(LinearPortal);🆕 **隐式赋值** `destObj = partDestination` vs `:=` 显式;✅ `obj.&Method.Program` SOP **第 8 次跨 Station 验证完全可移植**;✅ 5/7 Station 有 OnPull(MarkerCrossing 用 Frame-as-Semaphore 替代,AGVWithRobot 用 OnExit 替代)。| [2026-09-02-AllModels-station-onpull-meta-analysis.md](./2026-09-02-AllModels-station-onpull-meta-analysis.md) |
| 2026-09-02 | XZYStacker | stacker-crane-deepdive | 🆕 **7-Frame 集首例 Buffer + XYZ 三轴简化堆垛机**:14 子节点含 2 Place + 1 Buffer + 2 Conveyor;完整 linear topology `Source→Conveyor(3m)→XZYStacker→Buffer(8 槽)→Station(10s)→Conveyor2(3.05m)→Drain`;🆕 **新 ICN**:`Buffer → NwIOBuffer`(IO buffer 内部实现);🆕 **XZYStacker Station.OnPull 完整 dump ~30 行**(SevenAxisRobot 60+ 行的简化版);🆕 **3D 关节链 2 种模式** — name-based `getObject("X") → x_Joint → getObject("Z") → z_Joint → getObject("Y") → y_Joint` vs prior SevenAxisRobot index-based `RobotBase/RobotBaseZ`;🆕 **`?.succ` 单 succ routing**(对比 prior MarkerCrossing `z_uniform` 随机);🆕 **2 Conveyor 不同 length**(3m vs 3.05m);✅ 6 Connector 连续无命名 gap(对比 prior UUID-stable gap 模式);✅ `obj.&Method.Program` SOP **第 7 次跨 Method dump 验证**;✅ Station.ProcTime=0 + PullCtrl=OnPull 是 stacker crane 标准 pattern;ICN 累计 14 个。| [2026-09-02-XZYStacker-stacker-crane-deepdive.md](./2026-09-02-XZYStacker-stacker-crane-deepdive.md) |
| 2026-09-02 | MarkerCrossing | userobjects-class-edges | 🆕 **`.UserObjects.AGV` 是 7-Frame 集首个用户自定义 class**(Factory51 `UserObjects/Production` 范式首例);`MarkerCrossing.AGVPool.AGV.~ = .UserObjects`(对比 AGVWithRobot `.StandardObjects`)— 同一概念 AGV 在不同模型用不同基类;🆕 **4 嵌套 corner cell 边缘各异**(位置感知模板):MarkerCrossing=5 edges(含 RightEntry/RightExit,south/center),MarkerCrossing2=4 edges(SW),MarkerCrossing3=4 edges(NE),MarkerCrossing4=4 edges(NW);✅ Destinations 部分 dump `d[1]=M1, d[2]=M2`(outer 4 Marker 是 Init/DestCtrl 随机派单目标);✅ DataList element print SOP `var x:=d[i]; print x`(避免直接 `print d[i]` 触发 bash 转义 Syntax error);✅ AGV 物理差异:本模型 Accel=0.4/Decel=1 vs AGVWithRobot Accel=0.8/Decel=0.5;✅ 第 4 次确认 Quirk #7 `z_uniform(1, Dim+1)` off-by-one。| [2026-09-02-MarkerCrossing-userobjects-class-edges.md](./2026-09-02-MarkerCrossing-userobjects-class-edges.md) |
| 2026-09-02 | MarkerCrossing | crossing-semaphore-deepdive | ⚠️ **7-Frame 集首个 Frame-as-Semaphore + 嵌套 cell 模板**(2×2 grid + 对角交叉):outer 26 + 4 nested Frame × 26 子节点 = **130 总对象**;🆕 **2 个新 ICN**:`DataList → NwRandom`(内部 random accessor 语义)+ `Method → Method`(无 Nw 前缀普通类);✅ **6 个 Method dump** — `obj.&Method.Program` SOP 跨 Frame-level(Init/DestCtrl)+ 嵌套 Frame(EntranceCtrl/ExitCtrl/RecalcLayout/Reset)全部成功;🆕 **callback 注入模式** — Init 显式 `agv.DestCtrl = &DestCtrl` 注入到 AGV.DestCtrl 属性(对比隐式 Station callback);🆕 **Frame-as-Semaphore** — 嵌套 Frame 内部 `Owner` Variable + `StoppingCounter` + EntranceCtrl/ExitCtrl 实现 AGV 路径交叉点互斥锁 + 3D `_3D.showGraphicGroup("Free"/"Busy")` 状态可视化;✅ Variable `.Value` API(实测 `TrackPitch.Value=2`、`Owner.Value=VOID`、`EntranceLength.Value=1.7`);✅ 完整 13 Connector 拓扑:4 嵌套 Frame 形成 2×2 cell + 4 对角交叉(右侧闭环 Connector2-5,左侧闭环 Connector6-9,对角 Connector10-13);🆕 Connector 命名 gap(无 Connector1,Connector 在末尾);✅ 第 3 次确认 Quirk #7 `z_uniform(1, Dim+1)` off-by-one(Init/DestCtrl/RecalcLayout 全用);✅ RecalcLayout 带 `param newValue:real` + `existsObject` 防御编程;ICN 累计 13 个。| [2026-09-02-MarkerCrossing-crossing-semaphore-deepdive.md](./2026-09-02-MarkerCrossing-crossing-semaphore-deepdive.md) |
| 2026-09-02 | SevenAxisRobot | onpull-dump | ✅ **成功 dump `.Models.SevenAxisRobot.SevenAxisRobot.OnPull` 完整 60+ 行 SimTalk 源码**(用户建议 `robot.&OnPull.Program` 语法,1 次成功):**完整 3D 七轴机器人动画控制** — 8 状态机(DrivingEmpty/Loading/DrivingFull/Damping/Unloading/Blocked/DrivingHome/Waiting)+ 重入保护 `if self.NumInExecution > 1 then return end` + MU 迭代 `?.FwBlockListEntry1` + 3D pose `_3D.Poses.moveTo(...)` + 阻塞重试 + `part.move(DestinationObject)` MU 移动 API + `RobotBase.moveTo(partPosition.x)` 关节链 + 阻尼 `wait Robot.DampingTime`;🆕 **Quirk #26**:`s.OnPull`(属性访问,fallback 报 FwBlockListEntry1)vs `s.&OnPull`(ref-operator,干净报 cannot find object)— `&` operator 才是正确 method 解析路径;**修正 prior onpull-attempt Quirk #21/#23/#24 大部分错判** — 实际 `?.FwBlockListEntry1` 是合法 MU iterator,不是内部别名;✅ 跨 session 验证:同样 `s.&OnExit.Program` 语法对 AGVWithRobot Station1 也工作(返 7 行简化派单);✅ 修正 prior RobotSet Quirk #7 `z_uniform(1, NumSucc+1)` 真实 off-by-one 本 session 再次确认;🆕 **dump Station callback SOP**:`obj.&MethodName.Program`(不依赖 path 寻址);Result: success。| [part1](./2026-09-02-SevenAxisRobot-onpull-dump-part1.md) / [part2](./2026-09-02-SevenAxisRobot-onpull-dump-part2.md) |
| 2026-09-02 | SevenAxisRobot | onpull-attempt | ❌ **OnPull 源码不可 dump**(simtalk_run 反射硬限制):PullCtrl="self.OnPull" 存在,str_to_obj(path) VOID / `PullCtrl.Program` 失效 / `&PullCtrl` ref-operator 编译错 / `s.OnPull` 报 FwBlockListEntry1 内部别名 / `s.FwBlockListEntry1=VOID` / read-library `probe_batch` 返 `_void: True`;🆕 **Quirk #20-25 一次性新增 6 条**(PS 2.0 callback method 反射盲区);🆕 PullCtrl **string/void 双重语义**(print 时字符串化,`var m:=s.PullCtrl` 赋值为 VOID);🆕 PS 2.0 **FwBlockListEntryN 内部编译别名**(s.OnPull 编译为 s.FwBlockListEntry1 但运行时 VOID);⚠️ **修正** prior branching-deepdive "NO_METHODS" 结论 — 实际"Frame 顶层无 Method,Station 上有 OnPull callback 但不可 dump";🆕 Station `numNodes/NumNodes/EntryCtrl` 全部 Unknown identifier(Station 不可枚举子节点,无 entry callback);**OnPull 实际代码逻辑未知,需 user GUI F8 查看或 expert 实跑仿真推断**;Result: partial。**⚠ 已被 onpull-dump session 修正 — `obj.&OnPull.Program` 语法可 dump,prior 12 次尝试方向错误**。| [2026-09-02-SevenAxisRobot-onpull-attempt.md](./2026-09-02-SevenAxisRobot-onpull-attempt.md) |
| 2026-09-02 | SevenAxisRobot | branching-deepdive | ⚠️ **7-Frame 集首例多出口分支拓扑**(prior 6 Frame 全单线):SevenAxisRobot Station1.ProcTime=0/ExitCtrl=VOID,**1 进 2 出**(Connector3→Conveyor→Drain, Connector4→ParallelStation→Drain3);✅ **NO_METHODS 纯声明式模型**(无 SimTalk,所有行为靠 Station/Conveyor/ParallelStation/Display 默认规则);🆕 **4 个新 ICN 发现**:`ParallelStation → Machine`、`Conveyor → Line`、`Display → NwDigitDpy`、`EventController → EventCtl`(无 Nw 前缀);✅ ParallelStation `XDim=2 / YDim=2 / Capacity=4 / ProcTime=10` 一致 baseline `ParallelStation/attributes`(`StartProcessingWhenFull`+`XDim`);✅ Conveyor `Length=3m / Speed=1m/s / Width=1m`;🆕 Display.Value="Waiting"(string 状态!ICN=NwDigitDpy 暗示 numeric 但实际 polymorphic);🆕 Drain3 同名复用模式(用户编辑删除 Drain/Drain2 后保留 Drain3,与 Connector 编号 gap 同源);⚠️ Quirk 候选 #18 batch attr probe 首个 fail 后静默跳过(必须逐 attr 单 probe)+ #19 Display polymorphic Value。ICN 累计 11 个:`Place/Network/NwSource/Drain/NwArc/NwMarker/NwAGVPool/Vehicle/EventCtl/Machine/Line/NwDigitDpy`。| [2026-09-02-SevenAxisRobot-branching-deepdive.md](./2026-09-02-SevenAxisRobot-branching-deepdive.md) |
| 2026-09-02 | AGVWithRobot | agv-dispatch-deepdive | ⚠️ **OnExit pull 派单 vs 仓库 RCS 中央调度**(baseline `warehouse-and-ctu-patterns.md §1-2` 11 DataTable 状态机):用户用 7 行代码完成单 AGV 派单;✅ Marker `general.md` 闭环 waypoint 模式成立;🆕 **Novel Connector UUID-stable 编号 gap**(Connector5-13,缺 1-4,PS 不重编号);🆕 **MStation* 实为 Marker 命名误导**(ICT=Marker/ICN=NwMarker,不是 Station);🆕 **AGV class 在 .StandardObjects**(ICN=Vehicle/ICT=Transporter,**不**在 .MaterialFlow);✅ 拓扑实测:`Source2→Conn5→Station1→(OnExit)→MStation1→Conn8→MStation2→Conn9→Marker→...→Marker4→Conn13→MStation1`(闭环);✅ Station1.ProcTime=2s(短触发)/Station2.ProcTime=10s(长加工);✅ AGV `Speed=1m/s, Accel=0.8m/s², Decel=0.5m/s²`;✅ `NumIdleAGVs` / `getIdleAGV` 是 baseline 合法 API;⚠️ Quirk 候选 #16 `.~` 行为双模(AGV.~=.StandardObjects 类库 vs Station1.~=.Models.AGVWithRobot 父 Frame)+ #17 `print &o.Program` 编译错(与 prior PortalCrane 反向);撤销 session 中途"AGVPool.NumAGVs 新 Quirk"误判——baseline `AGVPool/methods` 无此 API(只有 getAssignedAGV(No)/getAssignedAGVsTable/getIdleAGV);⚠️ `get-folder-tree`/`read-library` 硬编码 port 50007 在 50008 失效(沿 prior RobotSet 降级路径)。| [2026-09-02-AGVWithRobot-agv-dispatch-deepdive.md](./2026-09-02-AGVWithRobot-agv-dispatch-deepdive.md) |
| 2026-09-02 | PortalCrane | crane-deepdive | ⚠️ `.Models.PortalCrane.PortalCrane.InternalClassType=Station` 但 `.InternalClassName="Place"`(PS 2.0 标准工位类注册名重命名,PS Help 文档"Station"标题滞后);✅ 完整 `str_to_obj(".MaterialFlow.Station").InternalClassName="Place"` 验证;✅ Frame-as-Container ICN=`Network`(非通用 Frame);✅ 子对象清单 5 Place/Source/Drain + 4 Connector(NwArc);✅ Station-as-Crane 模式跨 2 个 7-Frame 模型成立(与 prior RobotComau 完全同构);⚠️ 与 prior 同样无 PickAndPlace/PortalCrane 内置类,无 crane 专属属性(CraneType/Speed/LoadTime/RangeX 全部 Unknown identifier);⚠️ 1 层简化 vs Factory51 3 层范式。新增 Quirk #13 `numChildren=0`(9 可见子对象仍报 0,Quirk #6 `numNodes` 同源扩展)、#14 `obj.Methods/Program/getAttribute("Methods")` simtalk_run 全部 Unknown identifier(Quirk #10 `$CustomAttributes` 全面扩展)、#15 `obj.~.~.~` 多重 ~ 链报"Arithmetic operations"(单 ~ 正常,多 ~ 失效)。**Novel**:PS 2.0 ICN 命名空间重构(Place/Network/NwSource/NwArc/NwObjFolder),建议 synthesizer 评审 PS Help 文档标题是否需更新。 | [2026-09-02-PortalCrane-crane-deepdive.md](./2026-09-02-PortalCrane-crane-deepdive.md) |
| 2026-09-02 | RobotComau | station-as-robot-deepdive | ✅ `.Models.RobotComau.RobotComau.InternalClassType=Station`(真非 PickAndPlace);✅ Station 标准属性(ProcTime/CycleTime/Capacity/Pause/failures/MTTR/ExitCtrl)全部 baseline matches;⚠️ 用户用 Station + 3D 图形替换而非 PickAndPlace,失去 Angles/Times Table/LoadingTime/UnloadingTime/TargetCtrl/PullCtrl;⚠️ 1 层无 `UserObjects/` 类库 vs Factory51 `Milling` 3 层范式。新增 Quirk #8 `ProcessingTime` → 真实 `ProcTime`、`#9` `isStation()` 不存在、`#10` `$CustomAttributes` simtalk_run 报 `$` syntax error、`#11` Station Entry/Exit 不可 SimTalk 读、`#12` PS 2.0 属性白名单(可访问 = Name/InternalClassType/~/Cont/NumMU/Pause/ProcTime/CycleTime/SetupTime/Capacity/failures/MTTR/ExitCtrl/Setup/NumChildren)。| [2026-09-02-RobotComau-station-as-robot-deepdive.md](./2026-09-02-RobotComau-station-as-robot-deepdive.md) |
| 2026-09-02 | Models-RobotSet(7 Frame) | robot-set-overview | ⚠️ 7 Frame 扁平化、无 `UserObjects/` 类库分层、无 RCS 集中控制(diverges vs Factory51 范式);✅ Station 作"机器人主体"语义载体合法、Frame-as-Container 嵌套合法(4 个 MarkerCrossing 嵌套 Frame);❓ Quirk #4 `ClassName` / #5 `array[1..6] := [string]` syntax error / #6 `Station.numNodes` 不可读 + simtalk_run 静默退出。3 个 Frame-level Method dump:MarkerCrossing.Init(DAGV dispatcher)、DestCtrl(on-exit 重新分配)、AGVWithRobot.OnExit(单 AGV 任务链)。 | [2026-09-02-Models-RobotSet-robot-set-overview.md](./2026-09-02-Models-RobotSet-robot-set-overview.md) |
| 2026-09-02 | Assembly1, Assembly2 | orientation | ✅ 与 Siemens Small-Parts-Production PSFM 同构(双装配线 + PreProduction 内嵌 + AS/MS/Robot/CrossTransfer 自定义类);⚠️ EventController 英文名 vs baseline 德语残留 `Ereignisverwalter`;❓ `write(0/1)` 通道编号语义未文档化。**用户叠加**: `.SimtalkClaude.{main,src,connection}` agent bridge + `.Models.internal.Admin` exam harness。Quirks: `m_recieve` 拼写错误、`main/` 与 `connection/` 双源副本、`MySocket.Host` 硬编码 IP。 | [2026-09-02-Assembly1,Assembly2-orientation.md](./2026-09-02-Assembly1,Assembly2-orientation.md) |

## 笔记结构(5 维 + 6 段)

每篇 session 笔记固定包含以下章节(未触发的维度/段写"本 session 无新增" / "本 session 无" + 一句话原因):

### 5 维(对照 `02-domain-know-how/` 主题结构)

1. `## 01-factory-know-how` — 工厂/仓库建模模式
2. `## 02-simtalkclaude-knowhow` — 桥协议相关观察
3. `## 03-modeling-know-how` — 通用建模(`01-objects` / `02-simtalk` / `03-software` 子节按需)
4. `## 04-modeling-example` — 可借鉴示例
5. `## 05-modeling-experience` — 经验沉淀(Quirk / 模式 / 反模式 / "原来 PS 是这样" 的洞察)

### 6 段(协议/索引段)

6. `## Cross-references` — 引用过的知识库条目 + 同模型 prior session + per-skill log + team-memory
7. `## Open questions / cross-pollination` — 未关闭问题 + 建议由 curator / skills-optimizer / synthesizer 评审的 finding
8. `## Operator self-review` *(可选,append-only)* — self-check 标记(`[ ]` checkbox 形式)

> 完整模板见 `agents/plant-simulation-student.md` "Session 笔记协议 / 模板" 段。

## 何时需要读本目录

- **新 student session 开始前**:命中某 Model + Scenario 组合的 prior session,先读避免重复扫描
- **curator 评估沉淀候选**:扫 `## Open questions / cross-pollination` 段,看是否有值得 append 到 `02-domain-know-how/` 的 finding
- **用户问"我们之前学过 X 模型吗"**:查索引表定位

## 与其他目录的关系

| 目录 | 内容性质 | 关系 |
|---|---|---|
| `04-agent-memory/student-memory/`(本目录) | student 产出的模型学习笔记 | **候选 finding 池**,供 curator 评估 |
| `04-agent-memory/plant-simulation-expert-memory/` | expert 的执行 session summary | **并行记忆**,与 student 同侧但维度不同(执行 vs 学习) |
| `02-domain-know-how/` | 已沉淀的领域知识(append-only) | **本目录的目标归宿**——由 curator 评审后沉淀 |
| `04-agent-memory/curator-memory/` | curator 的评审报告 | **本目录的治理输出**——读 curator 报告可追溯哪些 finding 被采纳/拒绝 |
| `skills/<x>/log/` | per-skill 调用日志 | student 写的 Quirk/cache 经验回传给 `skills-optimizer` 评审 |

## 重构元数据

- 创建日期:2026-09-01
- 创建执行者:plant-simulation-student(由用户指示新增)
- 创建原因:用户希望把"读模型"与"写模型"两条 agent 路径拆开,student 走只读扫描 + 5 维镜像分析,不污染 expert 的执行日志
- session 累计:4 篇(Assembly1+Assembly2 orientation / RobotSet overview / RobotComau station-as-robot deepdive / PortalCrane crane-deepdive)