# Cut list — `local-simtalk-get-class-inheritance/SKILL.md`

**Source:** `skills/local-simtalk-get-class-inheritance/SKILL.md` (272 lines / 13 471 bytes)
**Target:** ~195 lines / ~9 400 bytes (−28%)
**Patch format:** section-level bullets. Apply with `Edit` only after user confirms.

---

## Suggested deletions

### Cut #1 — Lines 56–78: "Skill convention: always announce with `infoBox`"

This is the longest of the 4 infoBox duplicates (~22 lines, includes the
`--no-infobox` NOT-supported warning). Replace with:

```markdown
### Skill convention: always announce with `infoBox`

Every invocation opens a non-modal `infoBox(text, false)` on the Plant
Simulation GUI before doing any work, and closes it (defensively twice)
on exit. See [`../local-simtalk-execution/references/infoBox-convention.md`](../local-simtalk-execution/references/infoBox-convention.md).

> ⚠️ `--no-infobox` is NOT supported on `probe_inheritance.py` (positional-
> args-only parser). See INH-7 below and
> [`cross-cutting-2026-08-27.md`](../optimizer-reports/cross-cutting-2026-08-27.md)
> Theme 2 for the cross-skill `--no-infobox` positional rule.
```

**Lines saved:** ~16

### Cut #2 — Lines 184–202: "Inheritance semantics (Plant Simulation)" → link

The 8-line `Origin` / `OriginRoot` / `Class` / `InternalClassType` table
plus 10-line example block is duplicated character-for-character in
`class-management/SKILL.md` lines 198–214. Replace with:

```markdown
## Inheritance semantics (Plant Simulation)

See [`../local-simtalk-execution/references/inheritance-semantics.md`](../local-simtalk-execution/references/inheritance-semantics.md)
for the authoritative `Origin` / `OriginRoot` / `Class` / `InternalClassType`
table and the `VOID` sentinel semantics.
```

**Lines saved:** ~18

### Cut #3 — Lines 35–55 (How it works) + 82–118 (Usage) — collapse pipeline

The 2-step protocol (probe / render) is described in full twice. Reduce
"How it works" to a 4-line overview; let Usage carry the detail.

**Lines saved:** ~12

### Cut #4 — Lines 94–110 (Python heredoc for filtering folder-tree)

The 17-line Python heredoc "filter the folder-tree JSON down to class
candidates" duplicates the heredoc in `read-library/SKILL.md` and the
`add-note-to-method` similar pattern. Move to a new helper script:

`scripts/filter_class_candidates.py <tree.json> <out.txt>`

Replace the heredoc with:

```bash
python3 scripts/filter_class_candidates.py \
  data/basis_tree_depth4.json paths.txt
```

**Lines saved:** ~14 (after 10-line new helper script)

### Cut #5 — Lines 203–225: Hard rules table — reduce to skill-specific

The 7-row inherited table collapses after `quirks-canonical.md` lands.

**Lines saved:** ~22

### Cut #6 — Lines 228–232: "Path resolution" → link

Replace with:

```markdown
## Path resolution

`str_to_obj(<path>)` follows Plant Simulation path convention. See
[`../local-simtalk-get-folder-tree/SKILL.md` §"Path resolution"](../local-simtalk-get-folder-tree/SKILL.md#path-resolution)
for the authoritative table.
```

**Lines saved:** ~4

---

## Net expected change

| Cut | Lines saved |
|---|---:|
| #1 infoBox convention block | ~16 |
| #2 inheritance semantics table | ~18 |
| #3 pipeline description dedup | ~12 |
| #4 Python heredoc filter | ~14 (after 10-line helper script) |
| #5 inherited quirks table | ~22 |
| #6 path resolution link | ~4 |
| **Total (net of new helper script)** | **~86** |

Post-patch target: ~186 lines / ~9 100 bytes.

---

## What to keep (do not cut)

- `When to use` / `Do not use this skill for` sections.
- INH-1..INH-7 quirks table (skill-specific, especially INH-7 about --no-infobox).
- "Output shape" JSON examples (operator-facing).
- "Limitations" section.

---

## Dependencies

- Cuts #1, #2, #5 require the cross-cutting shared refs
  (`cross-cutting-shared-refs.md`) to exist first.
- Cut #4 requires adding `scripts/filter_class_candidates.py`.

**Recommended order:**
1. Land cross-cutting shared refs.
2. Add `scripts/filter_class_candidates.py`.
3. Apply Cuts #1, #2, #5, #6, #3, #4 in that order.

---

*Generated by skills-optimizer, 2026-08-31. Verifies against full file read of `local-simtalk-get-class-inheritance/SKILL.md` lines 1–272.*