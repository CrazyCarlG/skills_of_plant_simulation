# Cut list — `local-simtalk-read-library/SKILL.md`

**Source:** `skills/local-simtalk-read-library/SKILL.md` (324 lines / 15 655 bytes)
**Target:** ~215 lines / ~10 400 bytes (−34%)
**Patch format:** section-level bullets. Apply with `Edit` only after user confirms.

---

## Suggested deletions

### Cut #1 — Lines 78–92: "Skill convention: always announce with `infoBox`"

Replace the 15-line block with a 2-line pointer:

```markdown
### Skill convention: always announce with `infoBox`

Every invocation opens a non-modal `infoBox(text, false)` on the Plant
Simulation GUI before doing any work, and closes it (defensively twice)
on exit. See [`../local-simtalk-execution/references/infoBox-convention.md`](../local-simtalk-execution/references/infoBox-convention.md).
```

**Lines saved:** ~13

### Cut #2 — Lines 251–268: "Method-object facts (from the knowledge base)" table

The 6-row table duplicates the authoritative attribute reference in
`01-plantsimulation-knowledge/.../Method/attributes/` and
`.../Method/read-only-attributes/`. Move to a new
`references/method-attrs-cheatsheet.md` and replace with a 3-line pointer:

```markdown
## Method-object facts (from the knowledge base)

The probe reads `Program`, `Encrypted`, `HasSyntaxError`, and `NumInExecution`.
For the full attribute reference, see
[`references/method-attrs-cheatsheet.md`](references/method-attrs-cheatsheet.md)
or the authoritative docs at
`01-plantsimulation-knowledge/.../Method/attributes/`.
```

**Lines saved:** ~17 (table) — but adds ~25 lines to the new cheatsheet doc, so net file-count neutral. **However**, the cheatsheet doc is now reusable across `add-note-to-method` (which also reads `Program`), so the duplication avoided in 2 SKILL.md files is ~25 lines.

### Cut #3 — Lines 45–76 (How it works) + 95–161 (Usage) — collapse pipeline description

The 3-step pipeline (Inventory / Probe / Render) is described in full twice.
Reduce "How it works" to a 5-line overview:

```markdown
## How it works

The skill is a **3-step pipeline**: (1) Inventory (folder-tree → filter
Methods), (2) Probe (batch read of `Program` + metadata), (3) Render
(aggregate into `library_dump.json`). See [Usage](#usage) for the CLI
invocation and output shape.
```

Keep the rest of Usage (the actual commands).

**Lines saved:** ~25

### Cut #4 — Lines 139–161: "Single-method shortcut" Python heredoc

The 22-line block embeds a 17-line Python heredoc for raw socket invocation.
Move into a `scripts/probe_single_method.py` helper and replace with:

```markdown
### Single-method shortcut

For dumping just one method, use [`scripts/probe_single_method.py`](scripts/probe_single_method.py):

```bash
python3 scripts/probe_single_method.py --path .Models.Model.init
```
```

**Lines saved:** ~20 (after factoring the helper script ~30 lines)

### Cut #5 — Lines 270–277: "Path resolution" → link

Replace with:

```markdown
## Path resolution

`str_to_obj(<path>)` follows Plant Simulation path convention. See
[`../local-simtalk-get-folder-tree/SKILL.md` §"Path resolution"](../local-simtalk-get-folder-tree/SKILL.md#path-resolution)
for the authoritative table.
```

**Lines saved:** ~5

### Cut #6 — Lines 225–248: Hard rules table — reduce to skill-specific

The 6-row inherited table (lifelines #6, #7, #13, modal, response framing, infoBox)
can collapse to one-line pointers after `quirks-canonical.md` lands.

**Lines saved:** ~20

---

## Net expected change

| Cut | Lines saved |
|---|---:|
| #1 infoBox convention block | ~13 |
| #2 Method-object facts table | ~17 (table) — but adds 25-line new doc reusable across 2 skills |
| #3 pipeline description dedup | ~25 |
| #4 single-method shortcut heredoc | ~20 (after 30-line helper script) |
| #5 path resolution link | ~5 |
| #6 inherited quirks table | ~20 |
| **Total (net of new helper script + cheatsheet)** | **~100** |

Post-patch target: ~224 lines / ~10 400 bytes.

---

## What to keep (do not cut)

- `When to use` / `Do not use this skill for` sections.
- LIB-1..LIB-7 quirks table (skill-specific).
- "Output shape" JSON examples (operator-facing).
- "Limitations" section.

---

## Dependencies

- Cuts #1, #6 require `infoBox-convention.md` + `quirks-canonical.md` to exist.
- Cut #2 requires adding `references/method-attrs-cheatsheet.md`.
- Cut #3 can land independently.
- Cut #4 requires adding `scripts/probe_single_method.py`.

**Recommended order:**
1. Land cross-cutting shared refs.
2. Add `references/method-attrs-cheatsheet.md` (shared with add-note-to-method).
3. Add `scripts/probe_single_method.py`.
4. Apply Cuts #1, #6, #2, #5, #3, #4 in that order.

---

*Generated by skills-optimizer, 2026-08-31. Verifies against full file read of `local-simtalk-read-library/SKILL.md` lines 1–324.*