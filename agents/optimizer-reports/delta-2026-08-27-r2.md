# Optimizer report — DELTA (re-pass #2) — 2026-08-27

**Date:** 2026-08-27 (same-day delta after 09:00 first pass)
**Operator:** skills-optimizer
**Trigger:** user asked "帮我优化下 skills" — selected "check log deltas, report only if new"

## Why this delta exists

The first pass on 2026-08-27 scanned 38 logs but **did not include** 7 newer logs that were written *after* the scan window closed (all file mtimes Aug 27 18:22; first-pass reports don't reference them by basename). This delta covers those 7 only.

| Skill | Logs added since first pass | New findings |
|---|---|---|
| `local-simtalk-execution` | 1 (`2026-08-27_ping-syntax-run-readlog-2.md`) | 1 (P1) |
| `local-simtalk-get-class-inheritance` | 1 (`2026-08-27_modelassistants-inheritance.md`) | 2 (P1, P1) |
| `local-simtalk-get-folder-tree` | 3 (`..._modelassistants-bfs.md`, `..._orientation-summary-from-fresh-data.md`, `..._orientation-summary-from-fresh-data-2.md`) | 4 (P2, P3, P1, P1) |
| `local-simtalk-read-library` | 2 (`..._modelassistants-probe.md`, `..._probe-methods-1path-and-3path.md`) | 2 (P1, P2) |

**Totals (this delta):** 0 P0 / 5 P1 / 2 P2 / 3 P3 — across 4 skills, 7 logs reviewed.

---

## Findings

### `local-simtalk-execution`

#### P1 — `.SimtalkClaude2.*` sibling namespace discovered
- **Quirk id candidate:** Q-EXEC-2 (or extend existing Quirk #9)
- **What:** Log `2026-08-27_ping-syntax-run-readlog-2.md` §3 ("simtalk_syntax") observed that the `simtalk_syntax` `log` field surfaced a stale JSON dump from a prior `bfs_one_level.py` probe against `.SimtalkClaude2.Objects` — an 8-child Folder (`Method`/`Socket`/`DataList`/`Dialog`/`HtmlReport`/`Variable`/`Button`/`DataTable`).
- **Why it matters:** The agent's hard rules protect `.SimtalkClaude.*` (legacy runtime namespace). `.SimtalkClaude2.*` is a **separate, similarly-named sibling namespace** used by SimTalkClaude2's runtime. Operators can confuse the two and accidentally write into the wrong one.
- **Evidence:** `skills/local-simtalk-execution/log/2026-08-27_ping-syntax-run-readlog-2.md` §3 (stale BFS JSON) + §"What this run validated / learned" bullet 1.
- **Suggested addition (for `lifelines.md` §6 or a new §A):** "Any path starting with `.SimtalkClaude` (with or without trailing digit, including `.SimtalkClaude2`) is off-limits for writes. Use the runtime it owns only via the documented protocol verbs (ping / simtalk_syntax / simtalk_run)."

#### P3 — confirms Quirk #9 (simtalk_syntax `log` is stale)
- Not new — already in the first-pass report. Re-validated in this run with a concrete example of stale content.

---

### `local-simtalk-get-class-inheritance`

#### P1 — `probe_inheritance.py` does NOT accept `--no-infobox` (correction to cross-cutting INDEX claim)
- **What:** Log `2026-08-27_modelassistants-inheritance.md` step 2 explicitly states: "Ran `probe_inheritance.py <paths.txt> <out.tsv>` (script does not support `--no-infobox` — infoBox auto-opened + closed by the script itself, per iron rule #2 still satisfied)." This is confirmed by the first-pass report for `local-simtalk-read-library/2026-08-27_modelassistants-probe.md` §"What this run validated / learned" final bullet.
- **Why it matters:** The first-pass cross-cutting INDEX theme #2 lists `probe_inheritance.py` under "Must be LAST (after subcommand/positional args)". That is **wrong** — the script does not accept the flag at all (positional-args-only parser). Users trying `probe_inheritance.py <paths> <out> --no-infobox` will get an error.
- **Correction to INDEX.md cross-cutting theme #2:**
  - ~~`local-simtalk-get-class-inheritance` (`probe_inheritance.py`): Must be LAST~~ → **`probe_inheritance.py`: flag NOT supported; script self-manages infoBox**
- **Evidence:** `skills/local-simtalk-get-class-inheritance/log/2026-08-27_modelassistants-inheritance.md` §Steps 2 + §"What this run validated / learned"; corroborated by `skills/local-simtalk-read-library/log/2026-08-27_modelassistants-probe.md` §"What this run validated / learned" final bullet.

#### P1 — pre-filter inheritance candidates to type in (Frame|Dialog|TableFile|Method)
- **What:** Log `2026-08-27_orientation-summary-from-fresh-data-2.md` ("orientation after THIRD model swap") reports 24 of 40 inheritance probes returned nothing because `Comment`/`DataTable` etc. don't expose `Origin` meaningfully.
- **Why it matters:** Saves 60% wasted TCP round-trips on every future orientation call. Currently no `SKILL.md` recipe exists for the pre-filter — operators have to discover this empirically.
- **Evidence:** `skills/local-simtalk-get-folder-tree/log/2026-08-27_orientation-summary-from-fresh-data-2.md` §Steps 4 + §"What this run validated / learned" bullet 4.
- **Suggested addition (for `local-simtalk-get-class-inheritance/SKILL.md` §Usage or `references/protocol-notes.md`):** "When extracting candidate paths from the BFS JSON, filter by `type in (Frame, Dialog, TableFile, Method, Part, Comment, HtmlReport, DropDownList, Table, DataList)` but expect 40-60% empty returns for non-Frame/Dialog/TableFile candidates. For a *clean* class hierarchy pass, pre-filter to `Frame|Dialog|TableFile|Method` only."

---

### `local-simtalk-get-folder-tree`

#### P2 — `bfs_one_level.py` rejects empty-string root
- **What:** Log `2026-08-27_modelassistants-bfs.md` step 2: "First probe `bfs_one_level.py ""` failed (`ERR: cannot resolve path: ""`) — empty-string root path is rejected. Switched to `"."` per SKILL.md usage example → success."
- **Why it matters:** A first-call stumble for any new operator. The SKILL.md example already uses `"."`, so the fix is essentially "follow the example", but the error message `"cannot resolve path: """` is unhelpful (doesn't suggest `"."` as the alternative).
- **Evidence:** `skills/local-simtalk-get-folder-tree/log/2026-08-27_modelassistants-bfs.md` §Steps 2 + §"What this run validated / learned" bullet 1.
- **Suggested SKILL.md addition:** Under §Usage / Quick start, add an explicit note: "The root path argument cannot be empty — use `"."` for basis root (not `""`)."

#### P3 — `*.fresh.json` cache convention formalized
- **What:** Log `2026-08-27_orientation-summary-from-fresh-data.md` §"What this run validated / learned" bullet 1: "before calling `bfs_full.py`, `ls data/` and look for `*_fresh.json` from the same session — use those instead."
- **Why it matters:** Saves 30-60 TCP round-trips and avoids GUI infoBox churn when the user is re-orienting within the same session/model. Already implicitly followed by other operators, now explicitly named.
- **Evidence:** `skills/local-simtalk-get-folder-tree/log/2026-08-27_orientation-summary-from-fresh-data.md` §"What this run validated / learned" bullet 1.
- **Suggested addition (for `SKILL.md` §When NOT to use this):** "If you already have `data/*_fresh.json` from the same loaded model in this session, **read those instead of re-running BFS**. Stale across model swaps — verify the `basis_tree_depth*_fresh.json` timestamp matches today's date."

#### P1 — `.SimtalkClaude.src.SimtalkAction.simtalkcode` body is a likely-typo stub
- **What:** Log `2026-08-27_orientation-summary-from-fresh-data-2.md` §"What this run validated / learned" bullet 3 reports that the method body is literally `var obj:=.createfodler` (22 bytes). `createfodler` is not a real Plant Simulation function — the real one is `createFolder`. Looks like a typo / template stub.
- **Why it matters:** If any future operator calls into `.SimtalkClaude.src.SimtalkAction.simtalkcode`, they will hit a syntax error at runtime. Currently dead code, but a latent landmine.
- **Evidence:** `skills/local-simtalk-get-folder-tree/log/2026-08-27_orientation-summary-from-fresh-data-2.md` §Layer A + §"What this run validated / learned" bullet 3.
- **Suggested action:** Document in `references/protocol-notes.md` as a known stub: "`.SimtalkClaude.src.SimtalkAction.simtalkcode` is a known stub containing a typo (`createfodler` vs `createFolder`). Do not call it as part of any workflow; the actual `simtalk_run` path goes through `SimtalkAction.simtalk_execute` (281 B)."

#### P3 — model rotation: warehouse → assembly → teaching in one day
- Informational. The first-pass cross-cutting INDEX theme #5 already flagged this. This delta adds concrete model names:
  - 09:37 warehouse: `.Models.Factory51` (142 children, WMS)
  - 10:20 assembly: `.Models.Assembly1` + `.Assembly2` (113 children each, conveyor + optimization)
  - 12:16+ teaching: `.Models.internal.Admin` (student grading / exam-No analyzer)
- **Suggested action:** Update cross-cutting INDEX theme #5 with these concrete paths.

---

### `local-simtalk-read-library`

#### P1 — TSV emitted by `probe_methods.py` has embedded newlines in `program` body (independent confirmation)
- **What:** Log `2026-08-27_modelassistants-probe.md` §"What this run validated / learned" bullet 1: "Probe script writes TSV with embedded newlines in the program body — naïve `csv.reader` split each Method across multiple rows. Recovery: split raw text by lines starting with `.` and having 8+ tab-separated fields, then accumulate body lines until the next record starts. After re-parse, only 1 row had a 1-char mismatch (`.Templates` actual=252 vs declared=251 — trailing newline). All 79 bodies recovered."
- **Why it matters:** This is **independent confirmation** of the P0 `render_library.py` multi-line-program-drop bug identified in the first-pass report. The bug is at the **parser/recovery** level, not just the renderer — anyone consuming the raw TSV needs the same line-based accumulator workaround. The `probe_methods-1path-and-3path.md` log (this skill, also today) uses `program_len == 70` + verbatim capture to confirm the *underlying* program string is correct, only the on-disk TSV framing is the problem.
- **Evidence:** `skills/local-simtalk-read-library/log/2026-08-27_modelassistants-probe.md` §"What this run validated / learned" bullet 1; corroborated by `skills/local-simtalk-read-library/log/2026-08-27_probe-methods-1path-and-3path.md` §Test 1 (program verbatim OK) + §"What this run validated / learned" bullet 3 (`log.replace("\\n","\n")` decoder works).
- **Promotion:** First-pass P0 was about `render_library.py` (renderer). This delta surfaces a **second consumer hazard** — anyone re-implementing a renderer must apply the same line-based accumulator workaround. → Recommend this become a **Quirk LIB-7** in `references/exploration-log.md`.

#### P2 — `--no-infobox` family inconsistency (now confirmed across 2 scripts)
- **What:** Both `2026-08-27_modelassistants-probe.md` (§"What this run validated / learned" final bullet) and `2026-08-27_modelassistants-inheritance.md` (under get-class-inheritance, same date) observe that `probe_methods.py` accepts `--no-infobox` but `probe_inheritance.py` does **not**.
- **Why it matters:** This delta's P1 finding for `local-simtalk-get-class-inheritance` is **strengthened** by an independent corroboration. The cross-cutting INDEX claim "either position works for `probe_methods.py`" remains accurate; the claim "must be LAST for `probe_inheritance.py`" is **definitively wrong** (flag not supported at all).
- **Evidence:** As above (both logs).
- **Suggested action:** Already covered above in `get-class-inheritance` P1.

---

## Verdict

**Actionability score (delta):** 0 P0 / 5 P1 / 2 P2 / 2 P3 — **delta-only**, not additive to first-pass totals.

**Recommended action:**
1. **Immediately correct INDEX.md cross-cutting theme #2**: `probe_inheritance.py` does not accept `--no-infobox` (script self-manages infoBox). The "must be LAST" claim is wrong.
2. **Add Q-EXEC-2** (or extend existing Quirk #9) to `local-simtalk-execution/references/lifelines.md`: any `.SimtalkClaude\d*` path is off-limits for writes.
3. **Add LIB-7** to `local-simtalk-read-library/references/exploration-log.md`: TSV `program` field has embedded newlines; consumers must use line-based accumulator, not `csv.reader`.
4. **Add inheritance candidate pre-filter** (Frame|Dialog|TableFile|Method) to `local-simtalk-get-class-inheritance/SKILL.md` or `references/protocol-notes.md`.
5. **Add `*_fresh.json` cache convention** to `local-simtalk-get-folder-tree/SKILL.md` §When NOT to use.
6. P2 / P3 items backlog.

**Per the iron rule ❶: this is a report only. No SKILL.md / script files were modified.**

---

## Cross-references

- First-pass INDEX: `agents/optimizer-reports/INDEX.md` (now stale on theme #2 — see correction above)
- First-pass reports:
  - `agents/optimizer-reports/local-simtalk-execution-2026-08-27.md`
  - `agents/optimizer-reports/local-simtalk-get-class-inheritance-2026-08-27.md`
  - `agents/optimizer-reports/local-simtalk-get-folder-tree-2026-08-27.md`
  - `agents/optimizer-reports/local-simtalk-read-library-2026-08-27.md`
- First-pass cross-cutting: `agents/optimizer-reports/cross-cutting-2026-08-27.md`
- 7 logs reviewed in this delta (all dated 2026-08-27, all under 9 KB):
  - `skills/local-simtalk-execution/log/2026-08-27_ping-syntax-run-readlog-2.md`
  - `skills/local-simtalk-get-class-inheritance/log/2026-08-27_modelassistants-inheritance.md`
  - `skills/local-simtalk-get-folder-tree/log/2026-08-27_modelassistants-bfs.md`
  - `skills/local-simtalk-get-folder-tree/log/2026-08-27_orientation-summary-from-fresh-data.md`
  - `skills/local-simtalk-get-folder-tree/log/2026-08-27_orientation-summary-from-fresh-data-2.md`
  - `skills/local-simtalk-read-library/log/2026-08-27_modelassistants-probe.md`
  - `skills/local-simtalk-read-library/log/2026-08-27_probe-methods-1path-and-3path.md`

---

## Operator self-review

- The `comm -23` filter initially produced too many "uncited" matches because the first-pass reports cite some files by shortform (`v1`, `v2`, ...). Cross-checked by reading the file inventory header in each report to identify the **truly** uncited logs. Result: 7 logs, all in this delta.
- Did **not** re-read the 38 first-pass-reviewed logs (would have wasted context). Stuck to log citations present in the existing reports + the 7 new files.
- Did **not** modify any SKILL.md / script / reference per iron rule ❶. Output is report-only.
- The "delta" wording (`r2`) was chosen over `delta-2026-08-27` to distinguish from any future same-day delta passes; future passes should use `r3`, etc.

---

*Generated by skills-optimizer, 2026-08-27 (re-pass #2). 7 logs reviewed.*